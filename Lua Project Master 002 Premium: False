local players = game:GetService("Players")
local message = "This Version Is Permanent Down Check Now The New Version!"

function Shutdown()
   for i, v in pairs(players:GetChildren()) do
    v:Kick(message)
end
end

Shutdown()
